---
overlay: 1.0.0
info:
  title: Overlay
  version: 0.0.0
actions:
  - target: "$"
    update:
      components:
        schemas:
          ForeignID:
            type: integer
  - target: "$.components.schemas.*.properties.*.oneOf[?(@.type == 'integer')]"
    update:
      "$ref": "#/components/schemas/ForeignID"
